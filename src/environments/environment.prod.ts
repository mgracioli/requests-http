export const environment = {
  production: true,
  API: '/'  //quando fizer o build de produção, a variável API que é o endereço para onde eu faço as requisições http não vai mais ser o localhost (o localhost só é usado para testes), mas sim '/' (tem q substituir esse '/' pelo endereço certo)
};
